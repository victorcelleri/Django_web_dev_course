from django.db import models


# ============================================================
# EJERCICIO — Define el modelo Recurso
#
# Prompt Cursor (Ctrl+K):
#   "Crea un modelo Django llamado Recurso con los campos:
#    titulo (CharField max 200), url (URLField),
#    descripcion (TextField, blank=True) y
#    creado (DateTimeField auto_now_add=True).
#    Agrega Meta con ordering=['-creado'] y
#    __str__ que devuelva self.titulo."
# ============================================================

# ESCRIBE EL MODELO RECURSO AQUÍ ↓


# ============================================================
# EJERCICIO EXTRA — Define el modelo Mensaje
#
# Campos: nombre, correo (EmailField), mensaje (TextField),
#         recibido (DateTimeField auto_now_add), leido (BooleanField default=False)
# ============================================================
